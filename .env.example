# ⚠️ PRIVATE KEY REMOVED FOR SECURITY
# This file no longer stores private keys
# Add your private key directly to hardhat.config.js when deploying

# NETWORK SWITCHING:
# Deploy to Base Sepolia Testnet:
#   npx hardhat run scripts/deploy.js --network base-sepolia
# 
# Deploy to Base Mainnet:
#   npx hardhat run scripts/deploy.js --network base

# SECURITY BEST PRACTICES:
# - Never commit private keys to version control
# - Use a separate wallet for deployments
# - Keep only necessary funds in the deployment wallet
# - Test on Base Sepolia testnet before mainnet deployment
