{
  "name": "base-dapp",
  "version": "0.1.0",
  "description": "Minimal Base dApp scaffold for mainnet deployment (SimplePayments)",
  "scripts": {
    "build": "npx hardhat compile",
    "compile": "npx hardhat compile",
    "test": "npx hardhat test",
    "deploy:base": "npx hardhat run scripts/deploy.js --network base",
    "deploy:base-sepolia": "npx hardhat run scripts/deploy.js --network base-sepolia"
  },
  "devDependencies": {
    "hardhat": "^2.16.0",
    "@nomiclabs/hardhat-ethers": "^2.0.6",
    "ethers": "^5.7.2",
    "dotenv": "^16.0.0",
    "chai": "^4.3.7"
  },
  "dependencies": {
  },
  "main": "hardhat.config.js",
  "directories": {
    "test": "test"
  },
  "keywords": [],
  "author": "",
  "license": "ISC"
}
